# VA-AI-CAC Requirements for Windows CPU
# Python 3.10 required
# Use this file for local development on Windows without GPU
#
# ✅ VERIFIED COMPLETE - Contains all packages needed by the application
# Coverage: 23/23 required imports (verified 2026-06-25)
#
# This file mirrors requirements.txt but with:
# - CPU-only PyTorch (no CUDA)
# - Compatible versions for Windows Python 3.10
# - Skips packages not available on Windows (cucim, openslide-python)
# - NumPy 1.x pinned (PyTorch 2.0.1 incompatible with NumPy 2.x)
# - OpenCV 4.11 pinned (newer versions require NumPy 2.x)

# Core Python packages
setuptools<81
wheel

# PyTorch (CPU only - no CUDA)
# Original: torch==1.12.1+cu113, torchvision==0.13.1+cu113
# Windows CPU: Using 2.0.1 for better compatibility
torch==2.0.1
torchvision==0.15.2
torchmetrics==1.5.2

# MONAI (core only, without [all] to avoid CUDA dependencies)
# Original: monai[all]==1.4.0
monai==1.4.0
einops>=0.6.0          # Required by MONAI SwinUNETR (not included in base install)

# Medical Imaging Libraries (matches requirements.txt)
pydicom==2.4.4
nibabel==5.3.2
SimpleITK==2.4.1
python-gdcm==3.0.24.1

# ITK (matches requirements.txt)
itk==5.4.0

# Data Science (matches requirements.txt where possible)
numpy==1.26.4,<2.0     # Pin to 1.x - PyTorch 2.0.1 incompatible with NumPy 2.x
pandas==1.3.4          # Same as requirements.txt
scipy==1.13.1
scikit-learn>=1.0.0,<1.6    # Original 0.24.2 doesn't build on Python 3.10
scikit-image>=0.19.0,<0.25

# Visualization (matches requirements.txt)
matplotlib==3.9.4
ipywidgets==7.8.1
opencv-python==4.11.0.86   # Pin to 4.11 - newer versions require NumPy 2.x
Pillow==11.1.0

# Web Framework (required by app.py)
Flask>=2.0.0
werkzeug>=2.0.0

# AWS SDK for S3 storage support
boto3>=1.35.0

# Utilities
tqdm>=4.62.0
psutil>=5.8.0
lmdb>=1.3.0
jupyter>=1.0.0
tensorboard>=2.8.0

# Note: The following packages from requirements.txt are SKIPPED for Windows CPU:
# - monailabel==0.8.5 (large dependency, optional for inference-only usage)
# - cucim (CUDA only, not available on Windows)
# - openslide-python (Linux only, requires system libraries)
#
# The application will run without these packages in CPU inference mode.

