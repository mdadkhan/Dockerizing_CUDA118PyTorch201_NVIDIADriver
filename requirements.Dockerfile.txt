# VA-AI-CAC Requirements (matches Dockerfile exactly)
# CUDA 11.8 + PyTorch 2.0.1 (without +cu118 suffix for pip install)
# Python 3.10 required
#
# Note: This file matches the Dockerfile installation order and versions.
# For the exact build sequence, see Dockerfile.

# Pin setuptools to avoid pkg_resources deprecation
setuptools<81

# PyTorch for CUDA 11.8 (compatible with NVIDIA driver 535+)
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.0.1
torchvision==0.15.2
torchmetrics==1.5.2

# Core numerical libraries
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.3.2
pandas==2.0.3
matplotlib==3.7.2
ipywidgets==7.8.1
Pillow==10.1.0

# Medical imaging libraries
itk==5.3.0
SimpleITK==2.4.1
nibabel==5.3.2
pydicom==2.4.4
python-gdcm==3.0.24.1
opencv-python==4.8.1.78

# MONAI - base package only (no [all] to prevent PyTorch upgrade)
monai==1.4.0
einops>=0.6.0

# MONAI optional dependencies
tensorboard
psutil
cucim
openslide-python
tqdm
lmdb

# Web framework (required by app.py)
Flask>=2.0.0
werkzeug>=2.0.0

# AWS SDK for S3 storage support
boto3>=1.35.0
